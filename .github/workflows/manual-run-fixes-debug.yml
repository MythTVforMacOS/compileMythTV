name: "Manual debug build (fixes - 33)"

on: [workflow_dispatch]

jobs:
  build-mythtv-fixes:
    uses: ./.github/workflows/build.yml
    with:
      MYTHBRANCH: "fixes/33"
      VERSNUM: 33
      DEBUGBUILD: true
    secrets: inherit
