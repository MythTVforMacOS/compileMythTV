name: "Build Mythfrontend - Fixes/32"

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the main branch
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
#  schedule:
#    - cron: '0 0 * * 0'
jobs:
  build-mythtv-fixes-32:
    uses: ./.github/workflows/build.yml
    with:
      MYTHVERS: 'fixes/32'
      VERSNUM: '32'
    secrets: inherit
